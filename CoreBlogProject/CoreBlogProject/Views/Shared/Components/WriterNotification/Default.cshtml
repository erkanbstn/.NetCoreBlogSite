@model List<Notification>


<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <h6 class="p-3 mb-0">Bildirimler</h6>
    @foreach (var uyari in Model)
    {
        <div class="dropdown-divider"></div>
        <a class="dropdown-item preview-item">
            <div class="preview-thumbnail">
                <div class="preview-icon bg-success">
                    <i class="mdi mdi-calendar"></i>
                </div>
            </div>
            <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                <h6 class="preview-subject font-weight-normal mb-1">@uyari.NotificationTitle</h6>
                <p class="text-gray ellipsis mb-0"> @uyari.NotificationDetails </p>
            </div>
        </a>
        <div class="dropdown-divider"></div>
    }
    <h6 class="p-3 mb-0 text-center"><a href="~/Writer/WriterNotification">Tüm Bildirimleri Gör</a></h6>
</div>